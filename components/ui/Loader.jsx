import { BarLoader } from "react-spinners";

const Loader = () => {
  return (
    <main className="min-h-dvh flex items-center justify-center">
      <BarLoader color="#8C00FF" />
    </main>
  );
};

export default Loader;
